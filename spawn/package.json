{
  "name": "pi-spawn",
  "version": "0.1.0",
  "description": "Delegate tasks to specialized subagents that run in isolated context, with streaming and parallelism.",
  "license": "AGPL-3.0",
  "private": true,
  "type": "module",
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "keywords": [
    "pi-package"
  ],
  "scripts": {
    "check": "tsc --noEmit",
    "test": "bun test",
    "clean": "echo 'nothing to clean'",
    "build": "echo 'nothing to build'"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-agent-core": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  },
  "devDependencies": {
    "pi-shared": "github:kazzarahw/pi-shared",
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
